'use strict';

describe('GamesChallengeController', function() {
  var gamesChallengeController;

  beforeEach(function() {
    Ember.run(function() {
      App.reset();
      gamesChallengeController = App.GamesChallengeController.create();
    });
  });

  it('', function() {

  });

  afterEach(function() {
    Ember.run(function() {
      gamesChallengeController.destroy();
    });
  });
});
